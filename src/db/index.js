    // minimal DB stub. Replace with Dexie or your existing implementation as needed.
    export function initDB() {
    // placeholder for indexeddb/dexie setup if needed
    return Promise.resolve()
    }
